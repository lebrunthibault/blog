{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "build": {
    "env": {
      "HUGO_VERSION": "0.139.3"
    }
  },
  "buildCommand": "npm install && hugo --gc --minify --ignoreCache",
  "framework": "hugo"
}
